<main class="posts-page container">
  <section class="posts">
    <% if @posts.any? %>
      <%# A Variável @posts no plural siginifica que vai ter um looping com o each 
      por debaixo dos panos chamando a partial _show.html.erb %>
      <%= render @posts %>
    <% else %>
      Nenhum post ainda.
    <% end %>
  </section>

  <aside>
    <%# <%= render current_user %>

    <section class="suggestions">
      <h4>Sugestões Para Você</h4>

      <%# <%= render @suggested_users %>
    </section>
  </aside>

  <footer>
    © <%= Date.current.year %> INSTAGRAM CLONE
  </footer>
</main>
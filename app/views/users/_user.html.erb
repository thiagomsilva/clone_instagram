<%= link_to user_path(user.username), class: "profile" do %>
  <%# Utilizando o helper user_avatar em application_helper  %>
  <%= user_avatar(user) %>

  <h5 class="username"><%= user.username %></h5>
  <p class="full-name"><%= user.name %></p>
<% end %>